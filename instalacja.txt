git clone https://github.com/maciek435/6DofRobotSim.git

python3.10 -m venv venv

source venv/bin/activate

pip install -r requirements.txt


uruchomienie:

na początku uruchomienia vscode raz!:
source venv/bin/activate musi być w terminalu (venv)

potem:

python3.10 app.py

otworzyć link hosta